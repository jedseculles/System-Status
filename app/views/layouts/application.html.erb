<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"
  "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <title><%= h(yield(:title) || "System Status") %></title>
    <%= stylesheet_link_tag 'default' %>
    <%= yield(:head) %>
  </head>
  <body>
    <div id="container">    
 	  <div id="heading">
			<%=h link_to 'System Status', root_url %>
		<ul>
			<% if signed_in? %>
				<li><%=h link_to 'HOME', root_url %></li>
				<li><%=h link_to 'SIGN OUT', sign_out_url %></li>
			<% else %>
				<li><%=h link_to 'HOME', root_url %></li>
				<li><%=h link_to 'SIGN IN', sign_in_url %></li>
			<% end %>
		</ul>
	  </div>
	
	  <%- flash.each do |name, msg| -%>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <%- end -%>
      
      <%= yield %>
    </div>
	<div class="copyright">Made by Dan.</div>
  </body>
</html>
