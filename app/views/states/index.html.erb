<% title "System Status ~ Home" %>
<% content_for :header do -%>
	System Status
<% end -%>

<ul id="states">
  <% for state in @states %>
    <li>
      <span><%=h state.title %></span>
      <span><%=h state.date %></span>
      <span><%=h state.status %></span>
<!--
      <td><%= link_to "Show", state %></td>
      <td><%= link_to "Edit", edit_state_path(state) %></td>
      <td><%= link_to "Destroy", state, :confirm => 'Are you sure?', :method => :delete %></td>
-->
    </li>
  <% end %>
</ul>

<% if signed_in? %>
<p><%= link_to "New State", new_state_path %></p>
<% end %>